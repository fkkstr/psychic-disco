<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redireciona todas as requisições que não são para arquivos
  # ou diretórios reais para o index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
